/**
 * Backend origin for auth API calls. Empty string = same origin (production
 * behind a reverse proxy). For split-origin local dev, set this to the API URL
 * (e.g. http://localhost:3001).
 */
export const AUTH_API_BASE_URL = "";
