# Naming rules
- All TypeScript types/interfaces prefixed with `I` (IRating, ICreateRatingInput).
- English everywhere: identifiers, comments, commit messages.
- Frontend files use type-specific extensions: .atom.tsx, .molecule.tsx, .organism.tsx.
- Enforced by: biome.json + tests/architecture/naming.arch.test.ts
